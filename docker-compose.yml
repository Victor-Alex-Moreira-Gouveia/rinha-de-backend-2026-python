services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./conf/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "9999:9999"
    depends_on:
      - api1
      - api2

  api1:
    build: .

  api2:
    build: .